version: 2

models:
  - name: stg_orders
    description: One Unique order per row
    columns:
      - name: orderid
        description: Primary key for stg_orders 
      - name: shipmode
        description: '{{doc("shipmode")}}'